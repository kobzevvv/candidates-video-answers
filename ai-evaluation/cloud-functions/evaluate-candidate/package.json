{
  "name": "evaluate-candidate",
  "version": "1.0.0",
  "description": "Google Cloud Function for evaluating candidate interview answers using OpenAI",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=evaluateCandidate",
    "deploy": "gcloud functions deploy evaluate-candidate --runtime nodejs18 --trigger-http --allow-unauthenticated --set-env-vars OPENAI_API_KEY=${OPENAI_API_KEY}"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.3.0",
    "openai": "^4.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}